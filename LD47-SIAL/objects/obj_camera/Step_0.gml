/// @description Deactivate Instances

/*
var extra = TILE_SIZE*2;

var borderx = camera_get_view_x(view)-extra;
var bordery = camera_get_view_y(view)-extra;

var borderw = camera_get_view_x(view)+camera_get_view_width(view)+extra;
var borderh = camera_get_view_y(view)+camera_get_view_height(view)+extra;

instance_activate_all();

with(obj_goat) {
	if (!(((x > borderx) and (x < borderw)) and ((y > bordery) and (y < borderh))))
		instance_deactivate_object(id);
}

with(obj_snowball) {
	if (!(((x > borderx) and (x < borderw)) and ((y > bordery) and (y < borderh))))
		instance_deactivate_object(id);
}

with(obj_brokenice) {
	if (!(((x > borderx) and (x < borderw)) and ((y > bordery) and (y < borderh))))
		instance_deactivate_object(id);
}
*/
